# Frontend v2 example environment
# Use /api in production when frontend and backend are served through the same reverse proxy.
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_EVALUATION_FORM_URL=https://forms.gle/REPLACE_ME
